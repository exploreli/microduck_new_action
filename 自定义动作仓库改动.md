# 自定义动作仓库改动 — 后空翻（Backflip）

> 本文档记录本次「给 microduck 添加**后空翻**训练」的全部仓库改动：具体文件位置、
> 代码所在行、模型训练启动方式、测试模型方式，并在末尾备注**另一个会话**对 mjlab
> 库文件 `velocity_command.py` 的改动。
>
> 任务 id：**`Mjlab-Backflip-Flat-MicroDuck`**　实验名：`microduck_backflip`
> 设计与实现细节见 `docs/superpowers/specs/2026-09-10-backflip-env-design.md`
> 与 `docs/superpowers/plans/2026-09-10-backflip-env.md`。

---

## 一、关键设计决策

后空翻是 `roulade`（前滚翻）的「空中版」兄弟。**唯一的物理差异——腾空 vs 支撑——
重塑了整套奖励结构**。下面是决定这个环境成败的关键设计（每条都对应 AGENTS.md 里
「用硬状态门编码动作本质、别靠小惩罚去劝」的教训）。

### 1. 空中旋转累加器（contact-inverted，核心反作弊）

- **决策**：翻转进度只在**机器人没有任何 geom 接触地面**时积分向后 pitch（`-ω_y`，
  `_BACKFLIP_ROT_SIGN=-1.0`）；前沿（max）单调不倒退，且 step-guarded（同一控制步多个
  奖励项读取不重复积分）。
- **为什么**：roulade 是支撑滚翻，累加器**接触门控**（只有在地上转才算）；后空翻是空中
  翻转，必须**接触反转**（只有在空中转才算）。若沿用 roulade 的门控方向，policy 会在地上
  向后滚刷满 2π 而从不跳起来。这条把「后空翻 = 腾空」的物理本质直接编码进进度计量。
- **附带门控**：矢状面 flatness smoothstep 门——侧手翻/肩翻把横轴甩离水平会被折扣到 0，
  只有干净的矢状面翻转计数；并 latch「本 episode 曾离地」，从未真正腾空的小跳收不到落地年金。
- **代码**：`mdp.py:7283 _update_backflip_accum`、`7271 _backflip_airborne`、`7317 _backflip_completion_gate`。

### 2. 反向课程（空中 spawn）

- **决策**：一部分 episode 从**空中**开始——已团身、已后翻 120°–330°、带向后角动量、
  trunk z 高于站立、累加器预置到 spawn 角、airborne latch=True。混合比由课程调度：
  站立/空中 `0.50/0.50 → 0.65/0.35 → 0.80/0.20`（step 0 / 3000·24 / 6000·24），空中永不为 0。
- **为什么**：后空翻的成败在**后半程**（展体 → 看清地面 → 落地）。若只从站立起跳，on-policy
  数据几乎全花在起跳段，「最后一公里」永远练不到（「学会开头、学不会收尾」）。空中 spawn 从
  第 0 迭代就给落地段稠密数据，同时是真实的 DR。
- **代码**：`mdp.py:7339 reset_backflip_state`；课程 `backflip_spawn_mix`（env cfg `611–621` 行）。

### 3. 多层落地奖励（状态门控，非时钟）

- **决策**：落地/站立奖励全部**门控在翻转完成**（前沿 ≥ ~300°，`LANDING_GATE_LO/HI=300°/355°`
  的 smoothstep）**且**要求 airborne latch，分多层叠加：
  - `backflip_landing_composite`（6.0）：height × upright × pose 高斯**乘积**，宽 std（0.04/0.40/0.40）
  - `backflip_upright_after_flip`（2.0）+ `backflip_height_after_flip`（1.5）：远离目标处的线性/宽
    高斯 bootstrap（乘积 ≈0 时提供梯度）
  - `backflip_landing_sharp`（3.0）：紧 std（height 0.015、upright 0.3）乘积，逼它「站直」而非停在落地蹲
  - `backflip_stand_tax`（+5.0，自取负）：翻完后每步低于 `STAND_Z` 都扣分，「落地后瘫成一堆」由免费变净负
- **为什么**：
  - **门控在状态而非时钟**：「什么都不做」和站立 spawn 都赚不到，只有跳起来并翻转才解锁年金
    （绝不把正奖励门控在「摔倒/低」这种坏状态上，否则 policy 泊在最便宜的合格姿势刷分）。
  - **乘积 > 加和**：加和栈在目标态常有妥协盆（每项都拿 80% 靠一个 lean），高斯乘积在任何单一
    因子不足时就塌陷；但 std 要够宽让当前 policy 可见得分，否则梯度不可见。
  - **两层（宽 + 锐）**：宽层给远处梯度，锐层逼出「站直」的最后一公里。
- **代码**：`mdp.py:7491 / 7525 / 7546 / 7564 / 7593`。

### 4. 对称性损失（矢状面镜像）

- **决策**：`ENABLE_SYMMETRY=True`，`PpoWithSymmetryCfg + symmetry_cfg=SYMMETRY_CFG`，用 61D
  对称表做左右镜像增广。
- **为什么**：后空翻是矢状面/左右对称动作，镜像损失直接对抗**侧向塌陷（侧手翻）**这一主要失败
  模式，并近乎翻倍数据效率。（对照：单向旋转任务 `spin` 必须**关**对称，否则左旋被增广成右旋毁掉
  学习；后空翻双向对称，故**开**。）
- **冒烟实测**：`Mean symmetry loss: 0.0288`（在算）。
- **代码**：env cfg `MicroduckBackflipRlCfg`（`695–732` 行）。

### 5. 单一稠密进度信号（potential-based，防 jackpot / camping）

- **决策**：`backflip_progress`（8.0）支付 max-so-far 前沿的**增量**，翻满一圈总共支付 2π，原地
  camp 每步支付 0；并对**支付速率**封顶 `MAX_PAID_RATE=18 rad/s`。
- **为什么**：任何「到达 X 就每步付费」都是 jackpot，会买到任意暴力；potential-based（付 Δprogress）
  让「上升才付、保持不付」，不可刷。速率上限**高于**自然翻转（~12–16 rad/s），只没收荒谬甩鞭，绝不
  没收物理必需的旋转（roulade run-3「上限没收了整个动作」的教训）；超速税阈值 `OVERSPEED_OMEGA_MAX=22`。
- **代码**：`mdp.py:7466 backflip_progress`、`7617 backflip_overspeed_penalty`。

### 6. 发现期友好：motion-blockers ≈0 + 无无门控 |a_z|

- **决策**：`body_ang_vel=-0.002`、`angular_momentum=-0.001`（近乎 0）；**删除**常开 `upright`；
  **没有**无门控的 `trunk_vertical_accel_penalty`；settle 打磨项（`arrival_damping`、`joint_torque_rate_l2`）
  起步权重 0，由课程后期（2500·24 / 3500·24）才引入。
- **为什么**：翻转本身就是大角速度 + 大垂直加速度事件。任何有意义的 motion-blocker 权重或无门控
  |a_z| 都会在动作被发现前阻断它——「发现期任何尝试税都会让硬技能根本探索不出来，解药是**时机**而非
  幅度」。常开 upright 会与翻转直接对抗（旧尝试的核心失败）。

### 7. 符号约定（曾坑掉四个环境）

- **决策**：自取负惩罚（返回 ≤0，如 `backflip_stand_tax`）配**正**权重；正幅度成本（返回 ≥0，如
  `backflip_overspeed_penalty`）配**负**权重。
- **为什么**：给自取负惩罚一个负权重会双负号变成「奖励违规」，policy 会去刷它。铁律：每次 run 每个
  `Episode_Reward/<penalty>` 必须 ≤ 0（冒烟已逐项核对通过）。由 `test_already_negative_penalties_use_positive_weights` 锁。

### 8. 61D obs 契约（runtime 热切换）

- **决策**：actor obs 保持 **61D**（与 walking / standup / roulade 逐字节一致），不用的命令槽（head 4 /
  body 6）**零填充**，`twist` 命令中和到近零微噪声但**保留槽位**。
- **为什么**：真实部署热切换 ONNX 策略（walk / stand / trick）共享同一 obs 契约；删任何一个槽都会让
  ONNX 无法在 runtime 槽加载。由 `test_obs_parity_with_roulade_env` 锁。

---

## 二、本次改动清单（文件位置 + 代码所在行）

### 1. 新增文件（4 个）

| 文件 | 行数 | 内容 |
|---|---|---|
| `src/mjlab_microduck/tasks/microduck_backflip_env_cfg.py` | 732 行（全文新增） | 后空翻环境配置：常量、`make_microduck_backflip_env_cfg(play, rough)` 工厂、`MicroduckBackflipRlCfg` |
| `tests/test_backflip_cfg.py` | 521 行（全文新增） | 36 个 CPU 配置回归测试（无 sim） |
| `docs/superpowers/specs/2026-09-10-backflip-env-design.md` | 364 行 | 设计规范（中文，含实测验证结果） |
| `docs/superpowers/plans/2026-09-10-backflip-env.md` | 257 行 | 实现计划（中文，逐任务 + 陷阱备注） |

`microduck_backflip_env_cfg.py` 内部关键位置：

- **1–50 行**：模块 docstring（后空翻 vs 前滚翻的核心物理差异、速度/冲击对齐）
- **58 行**：`ENABLE_SYMMETRY = True`
- **60–128 行**：`ENABLE_*` 开关 + 全部常量（`EPISODE_LENGTH_S=4.0`、`STAND_Z=0.115`、
  `BACKFLIP_AIR_PITCH_MIN/MAX`、`AIR_Z_MIN/MAX`、`AIR_OMEGA_RANGE`、`LANDING_GATE_LO/HI`、
  `MAX_PAID_RATE=18.0`、`OVERSPEED_OMEGA_MAX=22.0`、`TUCK_OVERRIDES`、`_LEG_JOINTS`/`_NECK_JOINTS`）
- **~155–200 行**：三个接触传感器（`feet_ground_contact` / `self_collision` / `robot_ground_contact`）
- **~210–384 行**：18 项奖励集（删步行奖励 + 嫁接 backflip 奖励）
- **~386–478 行**：obs 61D 布局 + 命令中和
- **~480–596 行**：终止项 + 事件（BAM 展开、`set_backflip_state`、DR）
- **~602–690 行**：6 项课程
- **695–732 行**：`MicroduckBackflipRlCfg`

### 2. 修改文件（2 个）

**`src/mjlab_microduck/tasks/mdp.py`** — 新增 **479 行**，位于文件末尾
**第 7189–7667 行**（紧接既有函数 `roulade_lateral_velocity_penalty` 之后），即整个
backflip MDP 段。函数级行号：

| 行 | 符号 |
|---|---|
| 7250 | `_BACKFLIP_ROT_SIGN = -1.0`（向后翻 = body-frame 负 ω_y） |
| 7254 | `_BACKFLIP_AIR_SENSOR = "robot_ground_contact"`（空中门传感器名，LOAD-BEARING） |
| 7257 | `_backflip_state`（惰性建累加器状态） |
| 7271 | `_backflip_airborne`（= 无地面接触） |
| 7283 | `_update_backflip_accum`（**接触反转**累加器：仅腾空时积分 -ω_y，step-guarded） |
| 7317 | `_backflip_completion_gate`（前沿 smoothstep + airborne latch） |
| 7339 | `reset_backflip_state`（站立 + 空中两类 spawn，事件函数） |
| 7466 | `backflip_progress`（potential-based 前沿增量 + 支付速率封顶） |
| 7491 | `backflip_landing_composite` |
| 7525 | `backflip_upright_after_flip` |
| 7546 | `backflip_height_after_flip` |
| 7564 | `backflip_landing_sharp` |
| 7593 | `backflip_stand_tax`（**自取负** → 配正权重） |
| 7617 | `backflip_overspeed_penalty` |
| 7635 | `backflip_flatness_penalty` |
| 7650 | `backflip_sagittal_penalty` |
| 7661 | `backflip_lateral_velocity_penalty` |

> 复用的既有 helper（未改动，仅被引用）：`body_ang_vel_at_height`(763)、
> `standing_composite_score`(810)、`com_upward_velocity`(900)、`zero_command_padding`(5115)。

**`src/mjlab_microduck/tasks/__init__.py`** — 新增 **15 行**，分两处：

- **第 78–81 行**：导入
  ```python
  from .microduck_backflip_env_cfg import (
      make_microduck_backflip_env_cfg,
      MicroduckBackflipRlCfg,
  )
  ```
- **第 240–250 行**：注册任务（3 行注释 + `register_mjlab_task(...)`）
  ```python
  register_mjlab_task(
      task_id="Mjlab-Backflip-Flat-MicroDuck",
      env_cfg=make_microduck_backflip_env_cfg(),
      play_env_cfg=make_microduck_backflip_env_cfg(play=True),
      rl_cfg=MicroduckBackflipRlCfg,
      runner_cls=MicroduckOnPolicyRunner,
  )
  ```

> git 统计：`2 files changed, 494 insertions(+)`（`__init__.py` +15、`mdp.py` +479）；
> 另有 4 个未跟踪的新增文件（见上表）。

---

## 三、模型训练启动方式

> ⚠️ **本机前置**：uv 默认缓存属主为 `ubuntu` 且对 `dev_common` 只读，必须指向可写缓存，
> 否则 `uv sync` / `uv run` 会因权限失败。所有 uv 命令前先：
> ```bash
> export UV_CACHE_DIR=/home/dev_common/.cache/uv
> ```

**标准方式（AGENTS.md）：**

```bash
# ① 冒烟测试——正式训练前必跑（64 envs / 5 iters，几毛钱捕获 ~95% 配置错误）
uv run train Mjlab-Backflip-Flat-MicroDuck --env.scene.num-envs 64 --agent.max_iterations 5

# ② 正式训练
uv run train Mjlab-Backflip-Flat-MicroDuck --env.scene.num-envs 4096

# ③ 断点续训
uv run train Mjlab-Backflip-Flat-MicroDuck --agent.load-checkpoint model_XXXX.pt --agent.resume True

# ④ 提交到 Hugging Face Jobs（远程）
uv run train Mjlab-Backflip-Flat-MicroDuck --env.scene.num-envs 4096 --hf-jobs
```

**本机网络受限时的等价方式**（依赖已 `uv sync --offline` 装好，直接用 venv 内入口，
避免 uv 触发在线解析；wandb 用 `disabled` 免联网）：

```bash
WANDB_MODE=disabled CUDA_VISIBLE_DEVICES=0 \
  .venv/bin/train Mjlab-Backflip-Flat-MicroDuck \
  --env.scene.num-envs 64 --agent.max_iterations 5
```

- 日志目录：`logs/rsl_rl/microduck_backflip/<时间戳>_microduck_backflip/`
- checkpoint：`model_0.pt`、`model_<iter>.pt`（`save_interval=250`）
- wandb 项目：`mjlab_microduck`
- 预算：episodic 技巧约 1000 iters @ 4096 envs 起步；后空翻带逆向课程，预计需数千 iters。
- 训练时重点看：`Episode_Reward/backflip_progress` 是否上升、每个 penalty 是否 ≤ 0、
  空中 spawn 是否收敛到翻满 2π 并落回双脚。

---

## 四、测试模型方式

**① 配置回归测试（CPU，无需 GPU/sim）**

```bash
uv run --with pytest pytest tests/test_backflip_cfg.py -q
```

本机网络受限、pytest 不在缓存时，因这些测试都是无 pytest 特性的零参数纯函数，可用
venv 内 python + importlib 独立运行器执行（本次结果：**36 passed, 0 failed**）。

**② 冒烟训练验证**（同「三、①」）：判据 = 构建 / 无 NaN / obs 61D / 每奖励项计算 /
ONNX 导出。本次实测全部通过：actor obs `(61,)`、`nan_state: 0.0000`、18 奖励项全计算
且每个 penalty ≤ 0、`Mean symmetry loss: 0.0288`。

**③ 查看策略效果（play）**

```bash
uv run play Mjlab-Backflip-Flat-MicroDuck --wandb-run-path <entity/project/run_id>
```

**④ 导出 ONNX（normalizer 必须烘焙，唯一正确路径）**

```bash
# 标准：从 wandb run
uv run scripts/export.py Mjlab-Backflip-Flat-MicroDuck --wandb-run-path <...> --checkpoint N

# 本机：从本地 checkpoint（绕过 wandb）
.venv/bin/python scripts/export.py Mjlab-Backflip-Flat-MicroDuck \
  --checkpoint-file logs/rsl_rl/microduck_backflip/<run>/model_4.pt \
  --onnx-file out.onnx
```

本次实测：`onnx.checker` 通过，opset 18，**INPUT `obs[1,61]` → OUTPUT `actions[1,14]`**，
Actor MLP `61→512→256→128→14` 且 `EmpiricalNormalization` 已烘焙，部署元数据齐全。

**⑤ CPU 部署彩排（上真机前）**

```bash
uv run scripts/infer_policy.py --walking out.onnx      # BAM M6 执行器（与训练一致）
uv run scripts/infer_policy.py --walking out.onnx --no-bam   # XML PD
```

**⑥ 发布到 HF Hub（供 runtime `robotctl policy add` 加载）**

```bash
uv run publish --task Mjlab-Backflip-Flat-MicroDuck --wandb-run-path <...> \
  --checkpoint N --repo <user>/microduck-backflip --kind episodic --duration-s 4.0
```

---

## 附：验证状态一览

| 项 | 状态 |
|---|---|
| 代码就位（mdp.py + env cfg + 注册） | ✅ |
| CPU 配置测试 36/36 | ✅ |
| 冒烟训练 64envs/5iters（构建/无NaN/obs61D/18奖励项/符号约定） | ✅ |
| ONNX 导出（obs[1,61]→actions[1,14]，normalizer 烘焙） | ✅ |
| 中文设计文档 specs + plans | ✅ |
| 正式 4096-env 训练（验证「真的学得会」） | ⏳ 待跑 |
